#!/bin/sh

gtk-update-icon-cache -f /usr/share/icons/hicolor
rc-update del tor default 2>&1 > /dev/null || true

mkdir -p /var/lib/tor-applet/datadir
chown -R debian-tor:debian-tor /var/lib/tor-applet

#DEBHELPER#
